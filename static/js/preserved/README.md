# Preserved Question Cards frontend

This folder holds the previous Question Cards UI (`question-cards-app.js`) so gameplay,
filters, deck, and admin bank code are not lost while the public site shows Games as Coming Soon.

Backend APIs (`/api/...`), question bank data, and recommendation logic remain active in `app/`.
Do not delete this file until the Games experience is reconnected.
